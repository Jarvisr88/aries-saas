namespace Devart.Security.SSL
{
    using System;

    internal enum av : byte
    {
        public const av a = av.a;,
        public const av b = av.b;,
        public const av c = av.c;,
        public const av d = av.d;
    }
}

