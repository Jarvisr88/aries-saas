namespace Devart.Security.Ssh
{
    using System;

    internal enum an
    {
        public const an a = an.a;,
        public const an b = an.b;,
        public const an c = an.c;,
        public const an d = an.d;
    }
}

