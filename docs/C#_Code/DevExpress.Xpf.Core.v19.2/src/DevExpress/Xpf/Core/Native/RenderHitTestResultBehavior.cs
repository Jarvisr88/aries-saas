namespace DevExpress.Xpf.Core.Native
{
    using System;

    public enum RenderHitTestResultBehavior
    {
        public const RenderHitTestResultBehavior Stop = RenderHitTestResultBehavior.Stop;,
        public const RenderHitTestResultBehavior Continue = RenderHitTestResultBehavior.Continue;
    }
}

