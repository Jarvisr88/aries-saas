namespace DevExpress.Data.Helpers
{
    using System;

    public enum AsyncState
    {
        public const AsyncState Invalid = AsyncState.Invalid;,
        public const AsyncState Requested = AsyncState.Requested;,
        public const AsyncState Valid = AsyncState.Valid;
    }
}

