namespace DevExpress.Data
{
    using System;

    public enum ShowValues
    {
        public const ShowValues All = ShowValues.All;,
        public const ShowValues Same = ShowValues.Same;,
        public const ShowValues Different = ShowValues.Different;
    }
}

